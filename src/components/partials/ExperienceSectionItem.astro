---
const { company, betweenDates, excerpt, showMore, descriptionHTML } = Astro.props;
---
<section>
    <h3 class="text-xl font-semibold mb-4">{company}<br><span>{betweenDates}</span></h3>
    <p>{excerpt}</p>

    <details>
        <summary>{showMore}</summary>
        <div class="prose" set:html={descriptionHTML} />
    </details>

</section>